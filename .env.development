# 開発環境用 .env.development
# Docker Compose で起動するPostgreSQLに接続する設定
# このファイルは参考用です。実際には backend/.env と frontend/.env.local を使用してください

# === Database ===
# Docker Compose の postgres (ポート5433) を利用
# ユーザー: mycats / パスワード: mycats_dev_password (docker-compose.yml と一致)
DATABASE_URL="postgresql://mycats:mycats_dev_password@localhost:5433/mycats_development?schema=public"

# === JWT / Auth ===
JWT_SECRET="development-jwt-secret-at-least-32-characters-long-for-security-please-change-in-production"
JWT_EXPIRES_IN="15m"
JWT_REFRESH_SECRET="development-refresh-secret-at-least-32-characters-long-please-change-in-production"
JWT_REFRESH_EXPIRES_IN="7d"
PASSWORD_MIN_LENGTH=8

# === Admin User ===
ADMIN_EMAIL="admin@example.com"
ADMIN_PASSWORD="Passw0rd!"
ADMIN_FORCE_UPDATE=0

# === Server ===
NODE_ENV="development"
PORT=3004
CORS_ORIGIN="http://localhost:3000"

# === Argon2 Settings ===
ARGON2_MEMORY_COST=65536
ARGON2_TIME_COST=3
ARGON2_PARALLELISM=4
ARGON2_HASH_LENGTH=64
ARGON2_SALT_LENGTH=32

# === Rate Limiting ===
THROTTLE_TTL=60000
THROTTLE_LIMIT=100

# === Logging ===
LOG_LEVEL=debug

# === Health Check ===
HEALTH_CHECK_DATABASE=true
HEALTH_CHECK_MEMORY_THRESHOLD=0.9

# === Prisma ===
PRISMA_CLIENT_ENGINE_TYPE="library"

# === Authentication Bypass (開発専用) ===
# 非推奨: 一時的な開発用バイパス。撤去計画: docs/auth-disabled-deprecation.md 参照
AUTH_DISABLED=0

# === Optional Future Services (必要に応じてコメント解除) ===
# REDIS_URL="redis://localhost:6379"
# MAIL_HOST=smtp.example.com
# MAIL_PORT=587
# MAIL_USER=your-email@example.com
# MAIL_PASS=your-password
# AWS_ACCESS_KEY_ID=your-aws-access-key
# AWS_SECRET_ACCESS_KEY=your-aws-secret-key
# AWS_REGION=ap-northeast-1
# AWS_S3_BUCKET=your-bucket-name

