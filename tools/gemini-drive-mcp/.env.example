# Gemini API Key (Required)
# Get one here: https://aistudio.google.com/app/apikey (Free tier available)
GOOGLE_AI_API_KEY=your_api_key_here

# Google Drive Configuration (Auto-configured if paths are correct)
# If these files are in the project root, you don't need to change these.
# GOOGLE_DRIVE_OAUTH_TOKEN_PATH=../../.oauth-token.json
# GOOGLE_DRIVE_CREDENTIALS_PATH=../../oauth-credentials.json

# Folder ID (Auto-configured from GitHub Secrets workflow, but useful to set here for local testing)
# Copy this from your .folder_id file or GitHub Secrets
GOOGLE_DRIVE_FOLDER_ID=your_folder_id_here
